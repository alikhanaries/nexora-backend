/**
 * Compatibility infrastructure adapters (outbound HTTP clients, integration state) belong here.
 *
 * Step A establishes the boundary only — no outbound adapters or persistence are wired yet.
 */

export {};
